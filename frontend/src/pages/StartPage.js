import react from 'react';

const StartPage = () => {
    return (
      <div className="flex flex-col items-center h-screen gap-y-5 backgray-500">
        <h1 className="text-4x text-white font-bold">Start Page</h1>
        <p
className="text-sl t-lg bg-white filter shadow-ld p-4">Welcome to the Image-to-Text conversion service</p>
	          </div>
    );
};

export default StartPage;